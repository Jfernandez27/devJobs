<main class="contenedor">
    <form action="/jobs/updateJob/{{job.url}}" method="POST" class="default-form">
        <h3>General Information</h3>
        <div class="campo">
            <label for="title">Title</label>
            <input type="text" name="title" id="title" placeholder="Ex: React Developer" value="{{job.title}}" >
        </div>
        <div class="campo">
            <label for="company">Company</label>
            <input type="text" name="company" id="company" placeholder="Ex: Facebook" value="{{job.company}}" >
        </div>
        <div class="campo">
            <label for="location">Location</label>
            <input type="text" name="location" id="location" placeholder="Ex: England / Remote" value="{{job.location}}" >
        </div>
        <div class="campo">
            <label for="salary">Salary</label>
            <input type="text" name="salary" id="salary" placeholder="Ex: 1000 USD" value="{{job.salary}}"  >
        </div>
        <div class="campo">
            <label for="contract">Contract</label>
            <select name="contract" id="contract">
                {{#typeContract job.contract}}
                    <option value="" disabled selected>-- Choose --</option>
                    <option value="Freelance">Freelance</option>
                    <option value="Full Time">Full Time</option>
                    <option value="Part Time">Part Time</option>
                    <option value="By project">By project</option>
                {{/typeContract}}
            </select>
        </div>
        <h3>Job Description</h3>
        <div class="campo descripcion">
            <label for="description">Description</label>
            <input id="x" type="hidden" name="description" value="{{{job.description}}}">
            <trix-editor input="x" class="trix-content"></trix-editor>
        </div>
        <h3>Skills</h3>
        <ul class="lista-conocimientos">
            {{#selectSkills job.skills}}
            {{/selectSkills}}
        </ul>
        <div class="campo centrar-horizontal">
            <input type="hidden" name="skills" id="skills">
            <input type="submit" value="Save" class="btn btn-azul">
        </div>
    </form>
</main>